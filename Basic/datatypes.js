// number     range( -(2^53 − 1) and 2^53 − 1 )
let intNum=2611; // integer
let floatNum=26.11; // floating number
let Infinity=1/0; //special numeric value
let valNan=NaN; // not a number 

// bigInt 
// (Represents numbers larger than Number.MAX_SAFE_INTEGER (2^53 - 1))
 
let bigInt=748927942970257027287n;  //denoted by 'n' at the end of the numberr;
  
// String

let firstName="abhinav";
let lastName="srivastava"
let fullName= `${firstName +" "+ lastName}`;  //template literal



// Boolean 

let isjavasscriptFun= true;
let isjavasscriptBore= false;
 
//undefined 

 let username;    // when the value is not declared to the variable
 
 // Null

 let age=null; // empty value
 console.log(typeof null);  //In JavaScript, the typeof null is object due to a historical bug in the language.
 
 /*symbol (Represents a unique and immutable identifier)*/

 let uniqueId = Symbol('id');
   console.log(uniqueId);
   

